{
  "title": "Hermes Assist",
  "config": {
    "step": {
      "user": {
        "title": "Connect Hermes Assist",
        "description": "Connect Home Assistant Assist to Hermes Agent using the documented Hermes HTTP API. The integration builds /v1/chat/completions automatically.",
        "data": {
          "name": "Name",
          "api_host": "Hermes URL",
          "api_port": "Hermes API port",
          "api_token": "Hermes API token",
          "model": "Hermes API model ID",
          "timeout": "Request timeout seconds",
          "system_prompt": "System prompt"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to Hermes or validate the token.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "Hermes Assist is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Hermes Assist options",
        "description": "Tune Hermes Assist behavior. Connection URL and token remain in the original setup entry; the API model ID, timeout, and system prompt can be adjusted here.",
        "data": {
          "model": "Hermes API model ID",
          "timeout": "Request timeout seconds",
          "system_prompt": "System prompt"
        }
      }
    }
  }
}
